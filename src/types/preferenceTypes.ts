type BasicPreferenceType = {
  budget: number;
  style: string[];
  styleOptions: string[];
  description: string;
};

type VenueType = {
  budget: number;
  style: string[];
  description: string;
};

type PelaminType = {
  budget: number;
  style: string[];
  description: string;
};

type PhotographerType = {
  budget: number;
  style: string[];
  description: string;
};

type CateringType = {
  budget: number;
  style: string[];
  description: string;
};

type ClothingType = {
  budget: number;
  style: string[];
  description: string;
};

type MUAType = {
  budget: number;
  style: string[];
  description: string;
};

export type {
  BasicPreferenceType,
  VenueType,
  PelaminType,
  PhotographerType,
  CateringType,
  ClothingType,
  MUAType,
};
