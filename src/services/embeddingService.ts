import { PackageType, VendorType } from "@/types/basicTypes";

const embedVendor = ({ vendor }: { vendor: VendorType }) => {};
const embedPackage = ({ package }: { package: PackageType }) => {};

export { embedPackage, embedVendor };
